/**
 * Validate a cron expression.
 * @param expression The cron expression to validate.
 * @returns Whether the cron expression is valid.
 */
export declare const validateCronExpression: (expression: string) => boolean;
/**
 * Convert a time to cron time.
 * @param time The time to convert to cron time.
 * @returns The converted time.
 */
export declare const timeToCronExpression: (time: string) => string | null;
/**
 * Validate a time.
 * @param time The time to validate.
 * @returns Whether the time is valid.
 */
export declare const validateTime: (time: string) => boolean;
/**
 * Tests if a string is empty.
 * @param str The string to test.
 * @returns True if the string is empty, false otherwise.
 */
export declare const validateEmail: (str: string) => boolean;
/**
 * Tests if a username is valid.
 * @param str The username to test.
 * @returns True if the username is valid, false otherwise.
 */
export declare const validateUsername: (str: string) => boolean;
/**
 * Tests if a password is valid.
 * @param str The password to test.
 * @returns True if the password is valid, false otherwise.
 */
export declare const valiedatePassword: (str: string) => boolean;
/**
 * Tests if a phone number is valid.
 * @param str The phone number to test.
 * @returns True if the phone number is valid, false otherwise.
 */
export declare const validatePhoneNumber: (str: string) => boolean;
/**
 * Tests if a ipv4 address is valid.
 * @param str The IPv4 address to test.
 * @returns True if the IPv4 address is valid, false otherwise.
 */
export declare const validateIpv4Address: (str: string) => boolean;
/**
 * Tests if a color is valid.
 * @param color The color string to test.
 * @returns True if the color is valid, false otherwise.
 */
export declare const validateColor: (color: string) => boolean;
/**
 * Tests if a url is valid.
 * @param url The url to test.
 * @returns True if the url is valid, false otherwise.
 */
export declare const validateUrl: (url: string) => boolean;
/**
 * Tests if a path is valid.
 * @param path The path to test.
 * @returns True if the path is valid, false otherwise.
 */
export declare const validatePath: (path: string) => boolean;
/**
 * Tests if an image is valid.
 * @param image The image to test.
 * @returns True if the image is valid, false otherwise.
 */
export declare const validateImage: (image: string) => boolean;
//# sourceMappingURL=validate.d.ts.map